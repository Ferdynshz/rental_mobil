<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Detail Penyewa</title>
  <!-- Bootstrap CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light bg-gradient">
  <div class="container py-5">
    <div class="row justify-content-center">
      <div class="col-md-6">
        <div class="card border-start border-4 border-info shadow-lg">
          <div class="card-body">
            <h2 class="card-title text-info mb-4">Detail Penyewa</h2>
            <ul class="list-group list-group-flush mb-4">
              <li class="list-group-item">
                <strong>Nama:</strong><br>
                <%= penyewa.nama %>
              </li>
              <li class="list-group-item">
                <strong>Alamat:</strong><br>
                <%= penyewa.alamat %>
              </li>
              <li class="list-group-item">
                <strong>Nomor Telepon:</strong><br>
                <%= penyewa.nomor_telepon %>
              </li>
            </ul>
            <div class="d-flex justify-content-between">
              <a href="/penyewa/<%= penyewa.id %>/edit" class="btn btn-outline-warning">Edit</a>
              <form action="/penyewa/<%= penyewa.id %>/delete" method="post" class="d-inline">
                <button type="submit" class="btn btn-outline-danger">Hapus</button>
              </form>
              <a href="/penyewa" class="btn btn-secondary">Kembali</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
